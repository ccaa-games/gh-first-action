name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World"
      - name: Check Filesystem
        run:  | 
            ls -lha /
            ls -lha /home
      - name: Print goodbye
        run: echo "Done - bye!"
    
  
